// console.log(window)  run on browser where see all method of document

// const item = document.getElementById('head') // to access the id element 

// to find the attribut use 
// const item = document.getElementById('head').getAttribute('id')  --------------- to find the id in this id
// const item = document.getElementById('head').getAttribute('class') ----------------- to find the class in this id

const item = document.getElementById('head').setAttribute('class', 'test')  // if want to set attribute then put newattribute name and those name which also present previously value.
// otherwise otherwrite the vale and which new attribute put is only present in it.




/////////////////////////////////////

// item.style.color = 'red' or any styling attribute using style item


// **************************************///////////////////////////


// innerHtml  ** innerText  *** Textcontent


// in innerText the content which are visible on browser
// in Textcontent the content which write on the html page means sometime using style tag content hidden but in textcontent it visible.
// in innerHtml show text as well html tag if it inherite in


// time stamp : 20:00 min

//  document.querysSelector('#head)

//  in querySelector we select the item as we select the element in css

// in querySelectorALL('li') it return the Nodelist which is similar to array but  not a array.







console.log(item)